# -*- coding: utf-8 -*-
"""diabetes_prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1vh5dXMhRh322OM34pGQB-OemQKmcWrpD

Logistic Regression model for predicting Diabetes

This script loads Diabetes Dataset - Pima Indians dataset
and trains a logistic regression model, and evaluates its performance on test set based on accuracy metric
"""

import pandas as pd

data = pd.read_csv('data/diabetes.csv')

print(data.head())

# separate features and target variable (Outcome)

x = data.drop('Outcome', axis = 1)
y = data['Outcome']

from sklearn.model_selection import train_test_split

# split data into training and test sets 7:3
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.3, random_state=42)

from sklearn.linear_model import LogisticRegression

# create logistic regression model with max_iter 1000
logistic_regression_model = LogisticRegression(max_iter=1000)

# Train the model
logistic_regression_model.fit(x_train, y_train)

# Predict the Outcome on test data
y_pred = logistic_regression_model.predict(x_test)

from sklearn.metrics import accuracy_score

# calculate accuracy
accuracy = accuracy_score(y_test, y_pred)
print(f"Accuracy: {accuracy}")

